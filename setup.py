from setuptools import find_packages, setup

setup(
    name="scrapper",
    version="0.0.1",
    description="It's a ML project",
    author="user",
    author_email="user@example.com",
    packages=find_packages(),
    install_requires=[]
)